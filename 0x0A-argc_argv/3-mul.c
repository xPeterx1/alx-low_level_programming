#include <stdio.h>
#include <stdlib.h>
/**
 * main - print p name
 * @argc: arg count
 * @argv : aray of arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{

	if (argc == 3)
	printf("%i\n", atoi(argv[1]) * atoi(argv./add[2]));
	else
	printf("Error\n");
	return (0);
}
