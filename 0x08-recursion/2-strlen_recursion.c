#include "main.h"
/**
 * _strlen_recursion - good
 * @s: good
 * Return: i
 */

int _strlen_recursion(char *s)
{
	if (*s == 0)
	{
		return (0);
	}

	int i = 0;

	i++;
	return (i + _strlen_recursion(s+1);




}
